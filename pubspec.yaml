name: flutterapp
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.5.4
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  fluttertoast: ^8.0.9  # 请检查是否为最新版本

  #  加密本地存储数据
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  path_provider: ^2.0.11

  #  启动页
  flutter_native_splash: ^2.2.12
  #  网络请求
  dio: ^5.7.0
# 日志打印
  logger: ^2.5.0

  #  日志打印
  pretty_dio_logger: ^1.4.0
  #loading显示
  flutter_easyloading: ^3.0.5
  #GetX使用
  get: ^4.6.6
  #屏幕适配
  flutter_screenutil: ^5.9.3
#  轮播图片
  carousel_slider: ^5.0.0
#  加密
  encrypt: ^5.0.0
  pointycastle: ^3.0.0

  #  状态管理
  provider: ^6.0.1

#  定位功能
  flutter_amap_base: ^1.0.7
#  蓝牙
  flutter_blue_plus: ^1.4.0
  #nfc
  nfc_manager: ^3.5.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

#  启动页配置
flutter_native_splash:
  color: "#ffffff" # 背景颜色
  image: assets/images/splash_logo.png # 启动页的图片路径
  android: true
  ios: true
  fullscreen: true

flutter:
  assets:
    - assets/images/
    - key/



